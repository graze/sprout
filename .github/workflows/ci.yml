name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: PHP ${{ matrix.php }}${{ matrix.prefer-lowest && ' (lowest)' || '' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['7.0', '7.1', '7.2', '7.3', '7.4']
        prefer-lowest: [false, true]
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: composer:v2

      - name: Pin composer platform.php
        run: composer config platform.php "$(php -r 'echo PHP_VERSION;')"

      - name: Allow legacy deps with advisories (PHPUnit 5–7, symfony/process 2–4)
        # audit.block-insecure was added in Composer 2.7. PHP 7.0/7.1 ship
        # with Composer 2.2 (last minor that supports those PHP versions),
        # which rejects the unknown key — that's fine, 2.2 doesn't have
        # block-insecure to worry about anyway.
        run: composer config audit.block-insecure false || true

      - name: Install dependencies
        run: composer update --no-ansi --no-interaction --no-progress --no-scripts --prefer-dist ${{ matrix.prefer-lowest && '--prefer-lowest' || '' }}

      - name: Lint (phpcs)
        run: vendor/bin/phpcs -p --warning-severity=0 src/ tests/

      - name: Test (phpunit)
        run: vendor/bin/phpunit

  lint-md:
    name: Markdown
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint markdown
        run: make lint-md
